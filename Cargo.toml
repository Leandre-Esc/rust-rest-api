[package]
name = "rust-rest-api"
version = "0.1.0"
edition = "2024"

[dependencies]
actix-web = "4"
uuid = { version = "1", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }